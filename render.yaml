services:
  - type: web
    name: agent-arb
    env: node
    buildCommand: npm install --force && npm run build
    startCommand: node server/index.js
    envVars:
      - key: NODE_VERSION
        value: "22"
