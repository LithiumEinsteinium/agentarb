services:
  - type: web
    name: agent-arb
    env: node
    buildCommand: npm install --force && npm run build
    startCommand: npm run start
    envVars:
      - key: NODE_VERSION
        value: "22"
